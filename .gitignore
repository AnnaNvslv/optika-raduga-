# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Dependency directories
node_modules/
jspm_packages/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# dotenv environment variable files
.env
.env.*
!.env.example

# Astro build output / cache
dist/
.astro/

# OS files
.DS_Store
Thumbs.db

# Source material from the old Tilda site — reference-only, must stay local
# and never be committed (per the project brief: only extracted text/contacts
# were reused, none of Tilda's markup, scripts, styles or library files).
_reference/
*Tilda_ Радуга.html
*Tilda_ Радуга_files/
